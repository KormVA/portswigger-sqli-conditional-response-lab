SQL Injection Basics
🎯 Что такое SQL-инъекция?
SQL-инъекция (SQL Injection, SQLi) — это одна из самых критичных и распространённых уязвимостей веб-приложений. Она позволяет злоумышленнику вмешиваться в SQL-запросы, которые приложение отправляет к базе данных.

Проще говоря: если приложение некорректно обрабатывает пользовательский ввод, атакуящий может "внедрить" свой собственный SQL-код, который будет выполнен базой данных.

⚠️ Почему SQLi так опасна?
Успешная SQL-инъекция позволяет:

Читать конфиденциальные данные из БД (логины, пароли, персональные данные)
Модифицировать/удалять данные (удалить таблицы, изменить цены, добавить админа)
Получить полный контроль над сервером БД в некоторых случаях
Обойти аутентификацию ("админ" без пароля)

🔍 Как обнаружить SQLi?
Признаки возможной уязвимости:
Параметры в URL (?id=1, ?user=admin)
Формы входа, поиска, фильтров
Любые места, где пользовательский ввод попадает в SQL-запрос
Базовые тесты (для поиска):

sql
'                       -- простая кавычка (вызовет ошибку?)
"                       -- двойная кавычка
' OR '1'='1             -- всегда истинное условие
" OR "1"="1             -- то же для двойных кавычек
' AND '1'='1            -- также истина
📊 Основные виды SQL-инъекций
1. Классическая (Classic/In-band) SQLi
Атакующий использует тот же канал для внедрения и получения результатов.

1.1 Error-based (на основе ошибок)
База данных возвращает сообщения об ошибках, которые раскрывают структуру БД.

sql
' AND 1=CONVERT(int, (SELECT @@version)) --
Используется, когда приложение показывает ошибки БД пользователю.

1.2 Union-based (через UNION)
Используется оператор SQL UNION для объединения результатов легитимного запроса с данными атакующего.

sql
' UNION SELECT username, password FROM users --
Работает, когда результаты запроса отображаются на странице.

2. Слепая (Blind) SQLi
База данных не возвращает данные напрямую, но атакующий может делать выводы по поведению приложения.

2.1 Boolean-based Blind (на основе логических условий)
Атакующий задаёт вопросы типа "да/нет" и анализирует реакцию приложения.

sql
' AND (SELECT SUBSTRING(password,1,1) FROM users WHERE username='admin')='a' --
Если условие истинно → приложение ведёт себя одним образом, если ложно → другим.

2.2 Time-based Blind (на основе задержек)
Атакующий использует функции задержки (SLEEP(), WAITFOR DELAY) для определения истинности условий.

sql
' AND IF((SELECT 'a' FROM users LIMIT 1)='a', SLEEP(5), 0) --
Если условие истинно → приложение "засыпает" на 5 секунд.

3. Out-of-band SQLi
Результаты извлекаются через альтернативные каналы (DNS-запросы, HTTP-запросы на сервер атакующего).
Более редкий тип, требует специальных условий.

🎯 Простой пример эксплуатации
Исходный запрос приложения:

sql
SELECT * FROM users WHERE username = '[пользовательский_ввод]' AND password = '[пароль]'
Атака:

text
В поле логин вводим: admin' --
В поле пароль: что угодно
Итоговый запрос:

sql
SELECT * FROM users WHERE username = 'admin' --' AND password = 'что угодно'
Комментарий -- отрезает проверку пароля!

🛡️ Почему возникает уязвимость?
Основные причины:

Конкатенация строк при формировании SQL-запросов

python
# ❌ ОПАСНО
query = "SELECT * FROM users WHERE id = " + user_input

# ✅ БЕЗОПАСНО (параметризованные запросы)
query = "SELECT * FROM users WHERE id = %s"
cursor.execute(query, (user_input,))
Отсутствие валидации пользовательского ввода

Вывод ошибок БД на production-сервере

Избыточные привилегии у учётной записи приложения

🧪 Базовый чек-лист для тестирования
Поиск точек ввода:

GET/POST параметры

HTTP-заголовки (Cookie, User-Agent, Referer)

Файлы (загрузка с определённым именем)

Первичное тестирование:

sql
'                     -- проверка на ошибку
''                    -- возможно экранирование
' OR '1'='1           -- попытка обхода логики
' AND '1'='2          -- проверка на слепую SQLi
Определение типа БД:

sql
SELECT @@version      -- MySQL/MSSQL
SELECT version()      -- PostgreSQL
📈 Эволюция атаки (базовый сценарий)
text
1. Обнаружение (ввод ' → ошибка?)        → найден потенциальный вектор
2. Определение типа SQLi                 → Error-based / Union-based / Blind
3. Определение СУБД                     → MySQL / PostgreSQL / MSSQL / Oracle
4. Определение структуры БД             → имена таблиц и столбцов
5. Извлечение данных                    → логины, пароли, sensitive data
6. Эскалация привилегий (если возможно) → выполнение команд ОС
🚨 Важные термины
Payload — вредоносная строка, которая внедряется в запрос

Вектор атаки — способ доставки payload'а

Слепая инъекция — когда результаты не видны напрямую

Stacked queries — выполнение нескольких SQL-запросов подряд

Second-order SQLi — данные сохраняются и выполняются позже

✅ Ключевые выводы
SQLi существует там, где есть конкатенация пользовательского ввода с SQL-запросами

Всегда тестируйте поля ввода на базовые payload'ы

Blind SQLi требует больше времени и терпения для эксплуатации

Лучшая защита — использовать параметризованные запросы (prepared statements)

Ошибки БД на production — подарок для атакующего

📚 Что изучать дальше?
Union-based exploitation — научиться определять количество столбцов

Blind SQLi techniques — Boolean-based и Time-based атаки

Database-specific features — особенности разных СУБД

WAF bypass techniques — обход систем защиты

Automation with tools — SQLmap, Burp Intruder

💡 Помните: Практикуйтесь только на легальных полигонах (PortSwigger, TryHackMe, собственных стендах). Нелегальный тестирование — это преступление.
